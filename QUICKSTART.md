# راهنمای سریع شروع

## نقش سرورها

| سرور | محل | سرویس | توضیح |
|------|-----|--------|--------|
| **A** | ایران | `dnstt-client` | ترافیک کاربران اینجا دریافت می‌شود؛ کلاینت ترافیک را از طریق تونل به سرور B می‌فرستد. |
| **B** | خارج | `dnstt-server` | ترافیک دریافتی از A را دریافت می‌کند و به ۱۲۷.۰.۰.۱:پورت (MTProxy یا سرویس دیگر روی همین سرور) تحویل می‌دهد. پورت از کاربر پرسیده می‌شود. |

**جریان ترافیک:** کاربران → سرور A (dnstt-client) → تونل → سرور B (dnstt-server) → 127.0.0.1:پورت (مثلاً MTProxy)

## نصب یک‌کلیکی (ساده‌ترین روش)

**۱) سرور B (خارج)** — اول روی سرور خارج اجرا کنید. فقط دامنه و پورت سرویس مقصد روی همین سرور (مثلاً MTProxy) را وارد کنید:

```bash
sudo ./oneclick_external.sh
# هنگام سوال: دامنه + پورت مقصد روی 127.0.0.1 (پیش‌فرض 1080)
# یا با آرگومان: sudo ./oneclick_external.sh tunnel.example.com 1080
```

بعد از نصب، **کلید عمومی** را از خروجی کپی کنید (برای سرور A لازم است).

**۲) سرور A (ایران)** — بعد روی سروری که ترافیک کاربران را دریافت می‌کند اجرا کنید. دامنه، کلید عمومی (از B) و پورت گوش دادن برای کاربران:

```bash
sudo ./oneclick_iran.sh
# هنگام سوال: دامنه، کلید عمومی، پورت (پیش‌فرض 1080)
# یا: sudo ./oneclick_iran.sh tunnel.example.com "pubkey:xxxx..." 1080
```

کاربران در تلگرام/اپ پراکسی را روی **IP سرور A** و همان پورت (مثلاً 1080) تنظیم می‌کنند.

---

## روی سرور B (سرور تونل) — روش کامل

```bash
# 1. دانلود اسکریپت
wget https://raw.githubusercontent.com/your-repo/setup_dnstt.sh
chmod +x setup_dnstt.sh

# 2. اجرای اسکریپت
sudo ./setup_dnstt.sh
```

**اطلاعات مورد نیاز:**
- دامنه (مثال: `tunnel.example.com`)
- DNS resolver DoH (پیش‌فرض: `https://doh.cloudflare.com/dns-query`)
- IP سرور A (سرور دریافت کننده ترافیک از B - جایی که پراکسی تلگرام یا سایر اپ‌ها نصب است)
- پورت پراکسی/اپلیکیشن روی سرور A (معمولا `1080`)
- پورت محلی dnstt روی سرور B (پیش‌فرض: `5300`)
- پورت خروجی برای کاربران (پیش‌فرض: `1080`)

**توضیح:**
- **سرور B (خارج)**: dnstt-server ترافیک تونل را دریافت و به 127.0.0.1:پورت (مثلاً MTProxy روی همین سرور) تحویل می‌دهد.
- **سرور A (ایران)**: dnstt-client ترافیک کاربران را دریافت و از طریق تونل به B می‌فرستد.

**بعد از نصب سرور B:**
- کلید عمومی را از خروجی یا فایل `/opt/dnstt/server.pub` کپی کنید و هنگام نصب سرور A استفاده کنید.

## تنظیم DNS

رکورد A دامنه باید به **IP سرور B (خارج)** اشاره کند (کلاینت روی A از طریق همین دامنه به B وصل می‌شود):
```
Type: A
Name: tunnel.example.com
Value: IP_SERVER_B
TTL: 300
```

## برای کاربران نهایی

کاربران مستقیماً به **سرور A (ایران)** وصل می‌شوند (نه به کلاینت روی سیستم خودشان):

1. در تلگرام یا اپ: تنظیمات پراکسی → SOCKS5
2. Host: **IP یا دامنهٔ سرور A**
3. Port: **پورتی که هنگام نصب سرور A انتخاب کردید (پیش‌فرض 1080)**

اگر بخواهید کاربران روی سیستم خودشان کلاینت اجرا کنند (اتصال به 127.0.0.1)، از `client_connect.sh` یا دستور زیر استفاده کنید:
```bash
./dnstt-client -doh https://doh.cloudflare.com/dns-query -pubkey-file ./server.pub DOMAIN 127.0.0.1:1080
```
سپس در تلگرام: Host: 127.0.0.1، Port: 1080

## مدیریت سرویس

```bash
# استفاده از منوی تعاملی
sudo ./manage.sh

# یا دستورات مستقیم
sudo systemctl status dnstt-server
sudo systemctl restart dnstt-server
sudo journalctl -u dnstt-server -f
```

## عیب‌یابی

```bash
# بررسی وضعیت
sudo systemctl status dnstt-server

# بررسی پورت
sudo netstat -tulpn | grep 5300

# بررسی لاگ‌ها
sudo journalctl -u dnstt-server -n 50
```

